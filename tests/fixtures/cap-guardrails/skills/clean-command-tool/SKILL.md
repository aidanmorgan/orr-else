# clean_command_tool

Fixture SKILL.md for the `clean_command_tool` project tool.

Documents the raw-output archival strategy: complete stdout/stderr are persisted to
files; the model receives file references plus byte counts, never inlined raw text.
