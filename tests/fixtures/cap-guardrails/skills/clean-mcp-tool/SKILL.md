# clean_mcp_tool

Fixture SKILL.md for the `clean_mcp_tool` project tool.

The complete MCP callTool payload is persisted via persistMcpRawResult; the model
receives rawFile/rawBytes/rawChecksum references only.
